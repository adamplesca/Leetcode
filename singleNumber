//bit manipulation for SingleNumber

class Solution {
    public int singleNumber(int[] nums) {
        int result = 0;
        //^= is XOR for bit maipulating code (Like in Discrete Maths)
        for(int i = 0; i < nums.length; i++){
            result ^= nums[i];
        }
        return result;
    }
}
